# CNU Campus ChatBot (Sutra) — runtime dependencies
#
# chatbot.sh installs these automatically with uv from the cloned pyproject.toml.
# This file is provided for reference / manual setup. A Colab GPU (T4) runtime is
# recommended so llama-cpp-python uses the prebuilt CUDA wheel (no source build).

# Core runtime
pydantic
requests
pymupdf
huggingface_hub>=0.23

# RAG (Korean BM25)
kiwipiepy>=0.23.1
bm25s>=0.3.9
numpy>=1.26,<2.3

# Task 1 classifier (pinned to the classifier.joblib build version)
scikit-learn==1.7.2
joblib==1.5.3

# Chainlit UI
chainlit>=1.1.302

# llama.cpp Python bindings + server.
# On a Colab GPU runtime, install the prebuilt CUDA wheel so no source build runs:
#   pip install "llama-cpp-python[server]==0.3.28" \
#       --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121
llama-cpp-python[server]==0.3.28
